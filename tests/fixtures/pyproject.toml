[project]
name = "sample-py-project"
version = "0.1.0"
license = {text = "Apache-2.0"}
dependencies = [
  "httpx>=0.27.0",
  "pydantic>=2.7.0"
]

[tool.poetry]
name = "sample-py-project"
version = "0.1.0"
license = "Apache-2.0"

[tool.poetry.dependencies]
python = "^3.11"
fastapi = "^0.111.0"
